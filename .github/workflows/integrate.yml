name: Continue Inegration

on:
  pull_request:
    branches: [ v1 ]

jobs:
  pull_request_tester:
    runs_on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node_version: 24
      - run: npm ci
      - run: npm run start