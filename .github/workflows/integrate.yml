name: Continue Integration

on:
  push:
    branches: 
      - 'v1'
  pull_request:
    branches: 
      - '**'

jobs:
  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 25
      - run: npm ci
      - run: nohup npm start &
      - run: npm test