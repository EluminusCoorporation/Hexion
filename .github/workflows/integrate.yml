name: Continue Integration

on:
  pull_request:
    branches: [ v1 ]

jobs:
  pull_request_tester:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 24
      - run: npm ci
      - run: npm run start